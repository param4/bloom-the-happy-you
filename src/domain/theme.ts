/** The three selectable accent themes, chosen during onboarding. */
export type ThemeKey = 'terracotta' | 'sage' | 'blush';

export const THEME_KEYS: readonly ThemeKey[] = ['terracotta', 'sage', 'blush'];

export const DEFAULT_THEME_KEY: ThemeKey = 'terracotta';
